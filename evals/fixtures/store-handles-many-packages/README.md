# Store Handles Many Packages

This fixture uses the deterministic `store_many_packages` eval check. The check
creates a temporary SQLite store, inserts synthetic packages, and verifies
bounded summary output.
