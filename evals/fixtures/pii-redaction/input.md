# Input fixture

Synthetic source says a named customer and a credential were used in a handoff example. The expected artifact must keep structural facts only and replace sensitive values with redaction markers.
