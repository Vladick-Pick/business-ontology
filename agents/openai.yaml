interface:
  display_name: "Business ontology"
  short_description: "Operator skill for modeling how a business module really works"
  default_prompt: "Use $business-ontology to run a focused operator session to define, validate, or compile the business ontology. Resident-agent runtime and MCP server behavior are spec-only in this repo."
