{
  "id": "business-ontology-owner-chat-guard",
  "name": "Business Ontology Owner Chat Guard",
  "description": "Revises or suppresses unsafe resident-analyst owner chat without changing technical artifacts.",
  "version": "0.1.0",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "agentIds"
    ],
    "properties": {
      "agentIds": {
        "type": "array",
        "minItems": 1,
        "uniqueItems": true,
        "items": {
          "type": "string",
          "minLength": 1
        },
        "description": "OpenClaw agent ids whose human-facing outbound chat is guarded."
      }
    }
  }
}
